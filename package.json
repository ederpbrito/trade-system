{
  "name": "tradesystem",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev -w web\" \"npm run dev -w api\"",
    "build": "npm run build -w @tradesystem/shared && npm run build -w api && npm run build -w web",
    "test": "npm run test -w api && npm run test -w web",
    "lint": "npm run lint -w api --if-present && npm run lint -w web --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
